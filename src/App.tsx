import React from 'react';
import './App.css';
import { ThemeProvider } from 'styled-components';

const theme = {};

function App() {
	<ThemeProvider theme={theme}></ThemeProvider>;
	return <div className="App">UI INVENTORY</div>;
}

export default App;
